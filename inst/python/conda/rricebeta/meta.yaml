package:
  name: rricebeta
  version: "0.2"

source:
  fn: rricebeta-0.2.tar.gz
  url: https://pypi.python.org/packages/4a/e7/269a3d3fec17dc3185529cebed8cff845a01829d1f679467a59fed66de2f/rricebeta-0.2.tar.gz
  md5: fd9d192028e05dc07fe53bbc7efccfc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rricebeta = rricebeta:main
    #
    # Would create an entry point called rricebeta that calls rricebeta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - bs4
    - pandas
    - gzip
    - os
    - json

  run:
    - python
    - requests
    - bs4
    - pandas
    - gzip
    - os
    - json

test:
  # Python imports
  imports:
    - rricebeta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lagaillette/RRicePackage/tree/master/Python/RRiceBeta
  license: ICTLAB
  summary: 'RRice Package'
  license_family: OTHER

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
